function welcome(name: string) {
    console.log('welcome ' + name)

    console.log('hello world')
    const user = {
        name: 'Rafia',
    }

    const fname = user.name
    return name + fname
}

welcome('Rafia')
